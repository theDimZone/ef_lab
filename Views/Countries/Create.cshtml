@{
    ViewData["Title"] = "Добавление новой страны";
}

<div class="row">
    <div class="col">
        <h2>Добавление новой страны</h2>
    </div>
</div>
        <br />
<div class="row">
            <div class="col-6">
                @using (Html.BeginForm("Create", "Countries", FormMethod.Post))
                {
                    <div class="form-group">
                        <input type="text" class="form-control" placeholder="Название" name="Name">
                    </div>

                    <button type="submit" class="btn btn-primary">Отправить</button>
                }

            </div>
</div>
